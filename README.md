# Telegram-бот для аналитики по видео

Telegram-бот для аналитики по видео на основе задач на естественном языке. Бот принимает запросы на русском языке и возвращает результаты аналитики из базы данных.

## Возможности

- 📊 Аналитика по видео (просмотры, лайки, комментарии, жалобы)
- 📈 Отслеживание динамики (прирост метрик по часам)
- 👤 Фильтрация по креаторам
- 🤖 Понимание естественного языка (русский)
- 🔒 Безопасное выполнение SQL запросов

## Требования

- Python 3.10+
- PostgreSQL 12+
- Telegram Bot Token
- OpenAI API Key (или другой LLM провайдер)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd rlt-test-bot
```

2. Создайте виртуальное окружение:
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=your_telegram_bot_token
OPENAI_API_KEY=your_openai_api_key
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rlt_test_bot
DB_USER=postgres
DB_PASSWORD=your_password
```

5. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE rlt_test_bot;
```

## Загрузка данных

1. Скачайте JSON файл с данными (ссылка из задания)

2. Загрузите данные в базу:
```bash
python scripts/load_data.py path/to/videos.json
```

Или используйте модуль напрямую:
```bash
python -m src.database.loader path/to/videos.json
```

## Запуск бота

```bash
python main.py
```

## Использование

После запуска бота, отправьте ему команду `/start` в Telegram.

### Примеры запросов:

**Общая статистика:**
- "Сколько всего видео?"
- "Сколько просмотров у всех видео?"
- "Среднее количество лайков?"

**По креаторам:**
- "Сколько видео у креатора с id abc123?"
- "Сколько просмотров у креатора abc123?"

**Динамика:**
- "Какой прирост просмотров за последний час?"
- "Сколько новых лайков за сегодня?"
- "Прирост комментариев за последние 24 часа?"

**По конкретному видео:**
- "Сколько просмотров у видео с id xyz?"
- "Сколько лайков у видео xyz?"

## Структура проекта

```
rlt-test-bot/
├── main.py                 # Точка входа, Telegram бот
├── requirements.txt        # Зависимости
├── .env                    # Переменные окружения (не в git)
├── src/
│   ├── config.py          # Настройки приложения
│   ├── database/
│   │   ├── models.py      # SQLAlchemy модели
│   │   ├── db.py          # Подключение к БД
│   │   └── loader.py      # Загрузка данных из JSON
│   └── utils/
│       ├── llm.py         # Интеграция с LLM для генерации SQL
│       └── query_executor.py  # Выполнение запросов
└── scripts/
    └── load_data.py       # Скрипт загрузки данных
```

## Архитектура

1. **Модели данных** (`src/database/models.py`):
   - `Video` - итоговая статистика по видео
   - `VideoSnapshot` - почасовые замеры статистики

2. **LLM интеграция** (`src/utils/llm.py`):
   - Использует OpenAI API для преобразования естественного языка в SQL
   - Содержит детальное описание схемы БД для LLM
   - Валидация SQL запросов на безопасность

3. **Telegram бот** (`main.py`):
   - Обрабатывает команды `/start` и `/help`
   - Принимает естественно-языковые запросы
   - Выполняет запросы к БД и возвращает результаты

## Безопасность

- Все SQL запросы валидируются перед выполнением
- Разрешены только SELECT запросы
- Запрещены опасные операции (DROP, DELETE, UPDATE и т.д.)

## Технологии

- **aiogram** - Telegram Bot Framework
- **SQLAlchemy** - ORM для работы с БД
- **PostgreSQL** - База данных
- **OpenAI API** - LLM для обработки естественного языка
- **Pydantic** - Валидация настроек

## Лицензия

MIT